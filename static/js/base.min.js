"use strict";const loadScript = (src,attrs ={}) => new Promise((resolve,reject) =>{const script = document.createElement("script");script.src = src;script.defer = true;for (const [k,v] of Object.entries(attrs)){script.setAttribute(k,v);}script.onload = resolve;script.onerror = reject;document.body.appendChild(script);});const lazyLoadScripts = () =>{const{dataset}= document.body;const scriptQueue = [{src:"https:{src:"https:dataset.appUtilsUrl ?{src:dataset.appUtilsUrl}:null,dataset.appUrl ?{src:dataset.appUrl}:null ].filter(Boolean);if ("IntersectionObserver" in window){const observer = new IntersectionObserver((entries,obs) =>{entries.forEach(({isIntersecting,target}) =>{if (!isIntersecting) return;const{src,...attrs}= target.dataset;loadScript(src,attrs).catch(console.error);obs.unobserve(target);});},{rootMargin:"200px"});scriptQueue.forEach((script) =>{const placeholder = document.createElement("div");placeholder.dataset.src = script.src;for (const [k,v] of Object.entries(script)){if (k !== "src") placeholder.dataset[k] = v;}placeholder.style.display = "none";document.body.appendChild(placeholder);observer.observe(placeholder);});}else{scriptQueue.forEach(({src,...attrs}) => loadScript(src,attrs));}};const initTheme = () =>{const html = document.documentElement;const btn = document.getElementById("theme-toggle");const icon = btn?.querySelector("i");const THEMES = ["light","dark","bear"];const ICONS ={light:"fa-moon",dark:"fa-sun",bear:"fa-paw"};const applyTheme = (theme) =>{html.setAttribute("data-theme",theme);html.classList.toggle("dark",theme === "dark");localStorage.setItem("theme",theme);if (icon) icon.className = `fas ${ICONS[theme]}`;if (btn) btn.setAttribute("aria-label",`Toggle theme (current:${theme})`);if (html.__x?.$data) html.__x.$data.theme = theme;document.dispatchEvent(new CustomEvent("theme-change",{detail:{theme}}));};let storedTheme = localStorage.getItem("theme");if (!storedTheme) storedTheme = "bear";applyTheme(storedTheme);let themeIndex = THEMES.indexOf(storedTheme);if (themeIndex === -1) themeIndex = 0;const themeClickHandler = () =>{themeIndex = (themeIndex + 1) % THEMES.length;applyTheme(THEMES[themeIndex]);};btn?.addEventListener("click",themeClickHandler);const systemMedia = window.matchMedia("(prefers-color-scheme:dark)");const systemChangeHandler = (e) =>{if (!localStorage.getItem("theme")){applyTheme(e.matches ? "dark":"light");}};systemMedia.addEventListener("change",systemChangeHandler);window.addEventListener("unload",() =>{btn?.removeEventListener("click",themeClickHandler);systemMedia.removeEventListener("change",systemChangeHandler);});};let pageLoader = null;const hideLoader = () =>{pageLoader = pageLoader || document.getElementById("app-loader");if (!pageLoader) return;pageLoader.style.opacity = "0";setTimeout(() => (pageLoader.style.display = "none"),300);};const showLoader = () =>{pageLoader = pageLoader || document.getElementById("app-loader");if (!pageLoader) return;pageLoader.style.display = "flex";requestAnimationFrame(() => (pageLoader.style.opacity = "1"));};const setupPageTransitions = () =>{document.querySelectorAll("a[href]").forEach((link) =>{const url = new URL(link.href,window.location.origin);if (link.target || url.origin !== window.location.origin || url.hash) return;link.addEventListener("click",(e) =>{if (e.metaKey || e.ctrlKey || e.shiftKey || e.altKey) return;e.preventDefault();showLoader();setTimeout(() => (window.location.href = link.href),200);});});};const registerServiceWorker = () =>{if (!("serviceWorker" in navigator)) return;const swUrl = document.body.dataset.serviceWorkerUrl;if (!swUrl) return;window.addEventListener("load",() =>{navigator.serviceWorker.register(swUrl) .then((reg) => console.log("✅ ServiceWorker registered:",reg.scope)) .catch((err) => console.warn("❌ ServiceWorker failed:",err));});};const updateScrollProgress = () =>{const bar = document.getElementById("scroll-progress");if (!bar) return;const scrollTop = window.scrollY || document.documentElement.scrollTop;const scrollHeight = document.documentElement.scrollHeight - document.documentElement.clientHeight;const percent = scrollHeight > 0 ? (scrollTop / scrollHeight) * 100:0;bar.style.width = `${percent}%`;};const disableAnimationsForReducedMotion = () =>{if (!window.matchMedia("(prefers-reduced-motion:reduce)").matches) return;document.querySelectorAll(".particle,.animated").forEach((el) =>{el.style.animation = "none";el.style.transition = "none";});};const initBase = () =>{hideLoader();initTheme();setupPageTransitions();lazyLoadScripts();registerServiceWorker();updateScrollProgress();window.addEventListener("scroll",updateScrollProgress,{passive:true});disableAnimationsForReducedMotion();};document.addEventListener("DOMContentLoaded",initBase);// End of base.js