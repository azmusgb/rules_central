{
  "name": "rules_central",
  "version": "1.0.0",
  "description": "Flask-backed app; custom CSS only",
  "private": true,
  "scripts": {
    "dev": "npm run build:js",
    "build": "npm run build:js",
    "build:js": "node scripts/minify-js.mjs"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}